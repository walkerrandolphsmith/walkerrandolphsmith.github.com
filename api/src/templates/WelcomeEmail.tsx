import React from "react";

const WelcomeEmail = ({ email }) => (
  <html>
    <head>
      <title></title>
    </head>
    <body>
      <div>{email} reached out to you.</div>
    </body>
  </html>
);

export default WelcomeEmail;
